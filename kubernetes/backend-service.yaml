apiVersion: v1
kind: Service
metadata:
  name: todo-backend
  namespace: vedant-todo
  labels:
    app: todo-backend
spec:
  type: ClusterIP

  selector:
    app: todo-backend

  ports:
    - name: http
      port: 5000
      targetPort: 5000
      protocol: TCP