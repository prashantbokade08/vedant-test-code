apiVersion: v1
kind: Service
metadata:
  name: todo-frontend
  namespace: vedant-todo
  labels:
    app: todo-frontend

spec:
  type: ClusterIP

  selector:
    app: todo-frontend

  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP